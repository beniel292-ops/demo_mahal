{
  "name": "mahal-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Demo pitch site for a marriage hall — static pages + /api/enquiry serverless function.",
  "scripts": {
    "dev": "vercel dev",
    "serve": "npx serve .",
    "deploy": "vercel --prod"
  },
  "engines": { "node": ">=18" }
}
